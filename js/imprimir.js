import { subscribeAppearance, subscribeProducts, watchAdmin } from "./firebase-service.js";
let products = []; const page = document.querySelector("#printPage"); const locked = document.querySelector("#printLocked"); const category = document.querySelector("#printCategory"); const includeUnavailable = document.querySelector("#includeUnavailable"); const includePrices = document.querySelector("#includePrices");
function render() { const selected = products.filter((item) => item.visible !== false && (category.value === "Todos" || item.category === category.value) && (includeUnavailable.checked || item.available)); document.querySelector("#printProducts").innerHTML = selected.map((item) => `<article><img src="${item.image}" alt="${item.name}"><div><span>${item.code} · ${item.category}</span><h2>${item.name}</h2><p>${item.size || ""} · ${item.description || ""}</p><footer>${includePrices.checked ? `<strong>S/ ${Number(item.price).toFixed(2)}</strong>`:""}${item.available ? "":"<b>AGOTADO</b>"}</footer></div></article>`).join(""); }
let unsubscribeProducts = null;
watchAdmin((user) => { page.classList.toggle("hidden", !user); locked.classList.toggle("hidden", Boolean(user)); if (user && !unsubscribeProducts) unsubscribeProducts = subscribeProducts((items) => { products = items; render(); }, console.error, true); if (!user && unsubscribeProducts) { unsubscribeProducts(); unsubscribeProducts = null; } }); category.addEventListener("change", render); includeUnavailable.addEventListener("change", render); includePrices.addEventListener("change", render); document.querySelector("#printButton").addEventListener("click", () => window.print());
subscribeAppearance((appearance) => { if (appearance.logo) document.querySelector(".print-catalog>header img").src = appearance.logo; });
